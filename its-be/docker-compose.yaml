version: '3.1'

services:
  db:
    image: mysql
    restart: always
    environment:
      MYSQL_USER: its_admin
      MYSQL_PASSWORD: test123
      MYSQL_RANDOM_ROOT_PASSWORD: yes
      MYSQL_DATABASE: its-mysql
    network_mode: host
  kv:
    image: redis
    network_mode: host
